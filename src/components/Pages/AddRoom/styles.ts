import styled from "styled-components";

export const ContainerAddRoom = styled.section`
  margin-top: 30px;

  .container__wrapper {
    display: flex;
    flex-direction: column;
    gap: 30px;
  }
`;
