import styled from "styled-components";

export const ContainerSearchRoomState = styled.form`
  padding: 10px;
  width: 100%;
  background-color: var(--color-white);
  border-radius: var(--bd-radius);
  display: flex;
  align-items: center;
  justify-content: space-between;

  input {
    width: 100%;
    border: none;
    outline: none;
    padding: 14px;
    font-size: var(--sz-text1);
  }
`;
