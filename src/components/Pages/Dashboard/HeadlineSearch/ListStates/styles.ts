import styled from "styled-components";

export const ContainerListStates = styled.ul`
  height: 100%;
  max-height: 260px;
  margin-top: -8px;
  background-color: var(--color-white);
  border-bottom-left-radius: var(--bd-radius);
  border-bottom-right-radius: var(--bd-radius);
  overflow-y: scroll;
  position: relative;
  z-index: 10;
`;
